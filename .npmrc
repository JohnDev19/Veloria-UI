# Hoist build tool binaries into node_modules/.bin on every pnpm install.
# environments that use pnpm's isolated virtual store by default.
public-hoist-pattern[]=tsup
public-hoist-pattern[]=typescript
public-hoist-pattern[]=eslint
shamefully-hoist=false
