# Hoist tsup and typescript binaries to node_modules/.bin
# so `pnpm build` always finds them
# and similar environments that use isolated/virtual store mode.
public-hoist-pattern[]=tsup
public-hoist-pattern[]=typescript
public-hoist-pattern[]=eslint
shamefully-hoist=false
