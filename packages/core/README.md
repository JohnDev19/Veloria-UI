# @atlasui/core

> Build anything. Ship faster.

90+ production-ready React components — accessible, composable, dark-mode-ready.

## Install

```bash
npm install @atlasui/core
```

## Usage

```tsx
import "@atlasui/core/styles";
import { Button, Card, Input } from "@atlasui/core";

export default function Page() {
  return (
    <Card>
      <Input placeholder="Email address" type="email" />
      <Button variant="solid" loading={false}>
        Continue
      </Button>
    </Card>
  );
}
```

## Docs

Full documentation at [AtlasUI](https://github.com/JohnDev19/AtlasUI.git).
